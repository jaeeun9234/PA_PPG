<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 기본 테마 -->
    <style name="Theme.HeartSync" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- 스플래시 전용 테마 -->
    <style name="Theme.HeartSync.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_inset</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.HeartSync</item>
    </style>

</resources>