* 아마 laser 파장 하나만 쏜 상태로 얻은 데이터(?)
* hampel filter

  * N = 20;
  * nsigma = 2.5;



* red\_ac\_fft : 파장 하나만 사용해서 low frequency 부분 파형이 다르게 나오는지 확인해보기 \[교차검증]
* PI -> AC 계산

  * 기존 : max - min => 윈도우 변하기 전까지는 max-min 값이 이전 값으로 계속 유지됨.
  * 수정 : findpeaks 함수 사용 => peak와 trough를 직접 찾아낸 후 amplitude 도출

